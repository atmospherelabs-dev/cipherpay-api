services:
  api:
    build: .
    restart: unless-stopped
    env_file: .env
    environment:
      API_HOST: 0.0.0.0
      DATABASE_URL: sqlite:/data/cipherpay.db
    ports:
      - "127.0.0.1:3080:3080"
    volumes:
      - cipherpay-data:/data
volumes:
  cipherpay-data:
